import { areaIntegration } from './areaIntegration';
import { categoryIntegration } from './categoryIntegration';
import { statusIntegration } from './stateIntegration';

export const integrations = {
  area: areaIntegration,
  category: categoryIntegration,
  state: statusIntegration,
};
